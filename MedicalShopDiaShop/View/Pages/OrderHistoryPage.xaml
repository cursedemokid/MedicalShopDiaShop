<Page x:Class="MedicalShopDiaShop.View.Pages.OrderHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MedicalShopDiaShop.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="524" d:DesignWidth="1280"
      Title="История заказов">

    <Grid>
        <ListBox x:Name="OrdersListBox" Padding="42" Width="1280" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Width="1280" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Green" BorderThickness="2" Margin="20" CornerRadius="4">
                        <Grid Width="300" MinHeight="400" Margin="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" >
                                <TextBlock Text="{Binding OrderDate}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding DeliveryType}" FontSize="12"/>
                                <TextBlock Text="{Binding PaymentType}" FontSize="12"/>
                                <TextBlock Text="{Binding TotalCost, StringFormat={}Итого: {0:F2}}" FontSize="14" FontWeight="Bold" Margin="0,5,0,0"/>
                            </StackPanel>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding Products}" Margin="5">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="50"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <Image Grid.Column="0" Source="{Binding ImagePath}" Width="40" Height="40" Stretch="UniformToFill"/>

                                                <TextBlock Grid.Column="1" Text="{Binding ProductName}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="5,0"/>

                                                <TextBlock Grid.Column="2" Text="{Binding Quantity}" VerticalAlignment="Center" Margin="5,0" FontWeight="Bold"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
