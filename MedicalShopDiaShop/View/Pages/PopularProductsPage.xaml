<Page x:Class="MedicalShopDiaShop.View.Pages.PopularProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MedicalShopDiaShop.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="524" d:DesignWidth="1280"
      Title="PopularProductsPage">

    <Grid>
        <ListBox Name="ProductLb" Padding="42" Width="1280" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Width="1280" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="1*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Row="1" Grid.Column="1" Width="220" Height="350" Background="Green" Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*" />
                                <RowDefinition Height="0.7*" />
                            </Grid.RowDefinitions>
                            <Grid Background="White">
                                <Image Source="{Binding Image}"/>
                            </Grid>
                            <Grid Grid.Row="1" Margin="10" >
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="5" />
                                    <RowDefinition Height="40" />
                                    <RowDefinition Height="5" />
                                    <RowDefinition Height="10" />
                                    <RowDefinition Height="2" />
                                    <RowDefinition Height="1*" />
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="{Binding Cost}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                <TextBlock Grid.Row="2" Text="{Binding Name}" Foreground="White" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Top"/>
                                <TextBlock Grid.Row="4" Text="Есть в наличии" Foreground="White" FontSize="10"/>
                                <Grid Grid.Row="6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1*" />
                                        <ColumnDefinition Width="20" />
                                        <ColumnDefinition Width="1*" />
                                    </Grid.ColumnDefinitions>
                                    <Button Name="AddToBasketBtn" Background="LightGreen" Height="auto" Click="AddToBasketBtn_Click">
                                        <Button.Content>
                                            <TextBlock Text="В корзину" FontSize="15"/>
                                        </Button.Content>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
